intent:
  title: "Extract timeout logic into a shared helper"
  body: "The runner has duplicated timeout handling in run_implement_review_cycle and run_audit_report_flow. Extract the common pattern into a reusable helper."
expectations:
  relevant_files_contain:
    - "src/runner"
  plan_mentions:
    - "timeout"
  steps_mention:
    - "runner"
  has_implementation_steps: true
  min_relevant_files: 1
  complexity_is_one_of:
    - low
    - medium

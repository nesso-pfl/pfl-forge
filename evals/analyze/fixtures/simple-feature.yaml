intent:
  title: "Add --json flag to status subcommand"
  body: "Add a --json flag to the status subcommand that outputs the current state as JSON instead of human-readable text."
expectations:
  relevant_files_contain:
    - "src/main.rs"
  plan_mentions:
    - "json"
    - "status"
  steps_mention:
    - "clap"
  has_implementation_steps: true
  complexity_is_one_of:
    - low
